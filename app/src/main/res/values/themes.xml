<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ActionBar_Temas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/my_app_onPrimary</item>

        <!-- Definindo a cor da Status Bar -->
        <item name="android:statusBarColor">@color/my_app_primary</item>
    </style>

    <style name="Theme.ActionBar_Temas" parent="Base.Theme.ActionBar_Temas" />
</resources>